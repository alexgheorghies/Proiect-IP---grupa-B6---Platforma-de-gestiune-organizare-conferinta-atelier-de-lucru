{{velocity}}

#set($columns = ["ConferenceName", "NumberOfParticipants", "doc.creationDate","doc.creator","_attachments"])
#set($columnsProperties = {
   "ConferenceName" : { "type" : "text" , "size" : 20, "link" : "view"},
   "NumberOfParticipants" : { "type" : "number", "link" : "view"}
})
#set($options = {
  
  "className":"XWiki.ConferenceClass",
  "tagCloud" : true,
  "rowCount": 10
})

#if(!$isAdmin)
 $columns.add('_actions'))
 $columnsProperties.put('_actions', { 'actions' : ['delete','edit','rights'] }))
#end


#livetable("userdirectory" $columns $columnsProperties $options)

{{/velocity}

